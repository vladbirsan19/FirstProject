package week6.example7;

public class B< G, E> extends  A<G,E> {
    ////2 public class B<G, E> extends A<G, String> {

    //1 - type of B are passed to A types
    //2 - only one parameter of B(G) is passed to A
    // -the second one being of fixed type String

}
